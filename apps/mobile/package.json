{
  "name": "@artisans/mobile",
  "private": true,
  "version": "0.1.0",
  "main": "expo-router/entry",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "eas:configure": "npx eas-cli@latest build:configure",
    "eas:preview:android": "npx eas-cli@latest build -p android --profile preview",
    "eas:preview:ios": "npx eas-cli@latest build -p ios --profile preview",
    "eas:production": "npx eas-cli@latest build --profile production",
    "test": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@artisans/shared": "0.1.0",
    "@react-native-async-storage/async-storage": "^3.1.1",
    "@supabase/supabase-js": "^2.108.1",
    "expo": "latest",
    "expo-router": "latest",
    "expo-status-bar": "latest",
    "expo-video": "latest",
    "react": "latest",
    "react-native": "latest",
    "react-native-safe-area-context": "latest",
    "react-native-screens": "latest",
    "react-native-url-polyfill": "^3.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.7.0"
  }
}
